<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="350dp"
        android:layout_height="328dp"
        app:srcCompat="@drawable/logo"/>

    <Button
        android:id="@+id/button_on_shop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/imageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="I am on shop - need directions" />

    <Button
        android:id="@+id/btn_create_list"
        android:layout_width="234dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_on_shop"
        android:layout_alignStart="@+id/button_on_shop"
        android:layout_marginTop="17dp"
        android:text="Create a new shopping list" />

</LinearLayout>
